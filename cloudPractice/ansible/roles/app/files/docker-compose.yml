services:
  app:
    build: ./app
    container_name: todo_app
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      DB_HOST: "{{ db_host }}"
      DB_USER: todo_user
      DB_PASSWORD: todo_pass
      DB_NAME: todo_db
